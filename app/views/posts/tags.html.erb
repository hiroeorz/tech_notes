<main class="single-page">
  <nav class="breadcrumbs">
    <%= link_to t("posts.tags.home"), root_path %>
    <span>›</span>
    <span><%= t("posts.tags.title") %></span>
  </nav>
  <h1><%= t("posts.tags.title") %></h1>
  <div class="tag-cloud">
    <% @tags.each do |tag| %>
      <%= link_to posts_path(tag: tag.slug), class: "tag-pill" do %>
        <span><%= tag.name %></span>
        <strong><%= tag.respond_to?(:posts_count) ? tag.posts_count : tag.posts.count %></strong>
      <% end %>
    <% end %>
  </div>
</main>
