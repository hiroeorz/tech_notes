<main class="admin-page">
  <nav class="breadcrumbs">
    <%= link_to t("admin.categories.new.home"), root_path %>
    <span>›</span>
    <%= link_to t("admin.categories.new.admin"), admin_posts_path %>
    <span>›</span>
    <%= link_to t("admin.categories.new.categories"), admin_categories_path %>
    <span>›</span>
    <span><%= t("admin.categories.new.title") %></span>
  </nav>

  <h1><%= t("admin.categories.new.title") %></h1>

  <%= render "form", category: @category %>
</main>
