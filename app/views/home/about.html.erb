<main class="page-grid">
  <section class="content-area prose-panel">
    <nav class="breadcrumbs"><%= link_to t("home.about.home"), root_path %><span>›</span><span><%= t("home.about.title") %></span></nav>
    <h1><%= t("home.about.title") %></h1>
    <p><%= t("home.about.description_1") %></p>
    <p><%= t("home.about.description_2") %></p>
  </section>
  <%= render "shared/sidebar", categories: @categories, archives: @archives %>
</main>
